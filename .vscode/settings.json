{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "freelas_project.db",
            "database": "${workspaceFolder:Bot_freelas}/data/freelas_projects.db"
        }
    ]
}